//when the user clicks match, they have the option to view profile of the animal
import ViewProfileButton from "../buttons/ViewProfileButton";

export default function Undo() {
  const clickHandler = () => alert("clicked Match");
  return (
    < ViewProfileButton />
  );
}